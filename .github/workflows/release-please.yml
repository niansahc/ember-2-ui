name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      # Uses a PAT, not the default GITHUB_TOKEN. GitHub does not create
      # workflow runs from events raised by GITHUB_TOKEN, so with the default
      # the release PR gets no CI gate and the published release triggers
      # nothing downstream. Secret setup and scopes: ember-2
      # docs/RELEASE_WORKFLOW.md
      - uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
